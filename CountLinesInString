	//Ryan S. Hill 2018
	// Counting number of lines in a string
	
	//pre: none
	//post:returns the number of newlines in the given String
	public static int numberLines(String data)
	{
		int count=0;
		for(int i=0; i<data.length(); i++)
		{
			if (data.charAt(i) =='\n')
				count++;
		}
		return count;
	}